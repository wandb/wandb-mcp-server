## Conversation
User forwarded a diff-feedback file (.context/attachments/.../0-diff-feedback.md) with 5 review comments on PR #105 and said nothing else -> address them. This commit sits ON TOP of a93cee7 (the squashed feature commit) as a discrete review-response commit so the reviewer can read it against their inline comments and so the push is a fast-forward (no force-push needed).

## The 5 review items and how each was handled
1. agents.py L107-110 (truncation while-loop "confusing without context") -> added a 5-line comment explaining the geometric shrink: items vary in size so we re-measure tokens after each pass and drop ~10% of remaining items (>=1) from the end until the whole payload fits MAX_RESPONSE_TOKENS.
2. agents.py L150 ("keep try blocks tight; only try the thing that can fail") -> split the one broad try in _agents_request into TWO tight trys: one around the get_retry_session().post(...) round-trip, one around response.json(). The 404 / non-200 status-code branching now sits BETWEEN them, outside any try (it is plain control flow that cannot raise). Note: count_traces.py (the precedent) uses a broad try wrapping post+status+json; reviewer explicitly wants better than that, which also matches the user's CLAUDE.md "narrow try/except around the specific call". Kept `except Exception` (not narrowed to RequestException/JSONDecodeError) on purpose: this is the MCP tool boundary and must always return a structured_error envelope rather than propagate.
3. server.py L128 ("prefer dataclasses over tuples; more informative", "here and elsewhere") -> _AGENT_TOOLS was a tuple of positional 3-tuples; now a tuple of frozen slotted _AgentTool(name, impl, description) dataclass instances. Updated the _WEAVE_TOOL_NAMES spread (tool.name) and the register_tools loop (tool.name/.description/.impl). Added `from dataclasses import dataclass` + `Callable` to typing import. Scanned the rest of the PR diff for other record-like tuples: only _TRUNCATABLE_LIST_KEYS remains a tuple, which is correct -- it is a homogeneous immutable sequence of keys, not a record, so it stays a tuple.
4. server.py L384 "Available tools (22):" ("update the number") -> NOT changed. The PR already REMOVED the hardcoded "(22)" count (the line now reads "Available tools:" above a complete bullet list incl. the 8 agent tools). Re-introducing a literal count just reintroduces the rot the reviewer is reacting to -- the stale 22 is exactly that failure mode. This follows the user's CLAUDE.md "No stale counts in prose". Surfaced to the user with an offer to add an accurate number if they insist.
5. tests/test_hosted_runtime_guardrails.py L19 ("why change this?") -> answered + added a 3-line comment in FakeMCP.tool(). Reason: round-2 refactor (a93cee7) registers agent tools via mcp_instance.tool(name=..., description=...)(impl). The real FastMCP.tool() accepts name=; the FakeMCP double only accepted description=, so register_tools(fake) raised TypeError and failed all 10 tests in that file. The double now honors name= (falling back to func.__name__ for the @decorator form), mirroring the real contract.

## Verification
ruff check + format clean; 787 passed / 1 skipped. Registration sanity: real FastMCP still registers all 8 agent tools. Live: /tmp/verify_agents.py bcanfieldsherman/agents-demo -- all 8 tools returned real data after the try-block restructure (auth from ~/.netrc, never printed).

## Push state / open
- Local branch hatchery/reaper: a93cee7 (feature) -> ec61ead (this). Remote still at a93cee7; pushing this is a fast-forward (no force). NOT pushed yet (awaiting explicit go-ahead).
- Alternative the user may prefer: squash this into a93cee7 (amend) for a single clean commit -- would then need a force-push. Offered both.
