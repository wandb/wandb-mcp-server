# W&B MCP Server v0.4.1

**Release date:** Pending

## Availability

| Channel | State | Immutable artifact |
|---|---|---|
| Public source | Candidate | Pending signed tag and GitHub Release |
| W&B-hosted | Pending | Pending verified image digest |
| Dedicated/Self-Managed | Pending | Pending verified image digest and chart |
| Customer container | Pending | Pending verified public-registry digest |

## Summary

This patch preserves the v0.4 tool and runtime contract while repairing the
customer-container upgrade path from v0.3 charts. The release image translates
the old boolean tool selectors before starting the strict v0.4 runtime, so a
higher-priority legacy chart pin cannot make the new pod crash solely because
it rendered the former environment names.

## Runtime contract

<!-- BEGIN GENERATED: RELEASE RUNTIME CONTRACT -->
<!-- Generated by scripts/public_release.py docs. Do not edit this block. -->

This release replaces arbitrary per-feature booleans with one packaged runtime contract and four orthogonal selectors:

- `WANDB_MCP_TOOL_PROFILE` selects the exact product-capability profile.
- `WANDB_MCP_ACCESS_MODE` selects `read-write` or `read-only`.
- `MCP_WORKLOAD_PROFILE` selects query/history limits, admission mode and wait, deadlines, sessions, and HTTP rate policy.
- `MCP_CAPACITY_CLASS` selects bounded actor/process capacities and worker counts.

| Tool profile | Groups | Managed workloads | Read-write | Read-only |
|---|---|---|---:|---:|
| `models-only` | models | shared, dedicated | 17 | 15 |
| `models-weave` | models, weave | shared, dedicated | 22 | 20 |
| `models-weave-agents` | models, weave, agents | shared | 30 | 28 |
| `models-weave-agents-aria` | models, weave, agents, aria | local only | 33 | 30 |
| `models-weave-graphql-compat` | models, weave, raw-graphql | local only | 23 | 21 |

| Workload | Collection rows | History samples | Metric keys | Range span | Full-detail rows | Admission / HTTP rate |
|---|---:|---:|---:|---:|---:|---|
| `shared` | 100 | 500 | 20 | 5,000 | 3 | application admission and HTTP rate limiting off |
| `dedicated` | 250 | 1,500 | 50 | 20,000 | 10 | admission on; 60/key/minute, 1000/process/minute |
| `local` | 1,000 | 5,000 | 100 | 100,000 | 25 | application admission and HTTP rate limiting off |

| Capacity class | Actor | Process | Sync workers | Count workers |
|---|---:|---:|---:|---:|
| `small` | 4 | 4 | 4 | 4 |
| `medium` | 4 | 8 | 8 | 8 |
| `large` | 8 | 16 | 16 | 8 |

Runtime contract: `sha256:c871737fad4aa9d7b2d973d14d7063e2a17877e9f97c38608996dd49e812c5a0`.
<!-- END GENERATED: RELEASE RUNTIME CONTRACT -->

## Compatibility

- The public MCP tools, schemas, protocol support, workload policies, and
  profile manifests are unchanged from v0.4.0.
- Dedicated compatibility accepts strictly parsed legacy booleans only in the
  reviewed customer image. It maps classic Weave and read-only selections to
  the equivalent typed selectors, then removes the legacy variables before the
  Python runtime starts.
- Unsupported Dedicated capabilities such as Agent tools, ARIA, and the extra
  raw-GraphQL tool remain unavailable. Enabling one through an old flag fails
  startup rather than broadening the customer tool surface.
- Conflicting old and typed settings fail with a bounded configuration error.

## Operator changes

Pair this patch image with the compatible Operator chart through Managed Spec.
The image bridge also protects clusters where an existing CR or User Spec still
selects the preceding chart. Operators do not need to edit those resources by
hand for this patch rollout.

The chart-side compatibility bridge translates supported old values when the
new chart is effective. Unsupported or ambiguous overrides continue to fail
before Kubernetes mutation.

## Security and privacy

The bridge does not add tools, credentials, permissions, endpoints, or
low-level workload overrides. Boolean values are parsed from a fixed allowlist;
error messages name configuration fields but never include their values.

## Known limitations

- Agent, ARIA, and extra raw-GraphQL tool flags have no Dedicated mapping.
- Direct telemetry forwarding, pod-held telemetry credentials, opaque
  `envFrom`, and release-critical numeric overrides remain unsupported.
- Channel availability remains pending until the separately reviewed source,
  image, chart, and Managed Spec artifacts are published and verified.

## Validation

Pending current-head CI, installed-wheel profile checks, customer-image startup
tests under both legacy and typed chart environments, Helm rendering tests, and
QA rollout/rollback evidence.

## Immutable artifacts

- Signed source tag: Pending
- Source commit/tree: Pending
- GitHub Release: Pending
- Wheel and source-distribution checksums: Pending
- Public qualification/provenance attestations: Pending
- Hosted image digest: Pending
- Customer image digest: Pending
- Helm chart version: Pending

## Rollback

- Public source: fix forward from the signed tag.
- Managed: restore and verify the captured previous configuration and traffic.
- Dedicated/Self-Managed: restore the previous chart and image digest.
